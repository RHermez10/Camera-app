export interface signUpObj {
    username: string,
    password: string,
    admin: boolean
};

export interface loginObj {
    username: string,
    password: string
};

export interface photoObj {
    url: string,
    photographer: string
};
